Korean Reader sync-choice layout fix

Replace these files in the repository:
- index.html
- css/cloud.css
- js/cloud-sync.js

Then hard-refresh the website (Ctrl+Shift+R). For an installed PWA, close it fully and reopen it. If the old layout remains, clear the site's cached data or reinstall the PWA.
