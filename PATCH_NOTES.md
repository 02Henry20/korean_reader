# Korean Reader patch v5

Replace the matching files in the repository while preserving this directory structure.

Changes:

- Desktop word translations are positioned relative to the selected word.
- Card SVG discovery and JSON SVG fields are removed.
- Collection cards again use prominent character monograms.
- Stories without thumbnails use the collection monogram instead of generated book artwork.
- Existing external image thumbnails continue to work.

The old `assets/directory-svg/` directory can be deleted because it is no longer read by the app.
