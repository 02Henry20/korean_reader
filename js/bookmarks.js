/*
  Reserved for bookmark and reading-position persistence.
  The current application has no bookmark functionality, so this file intentionally
  contains no runtime behavior. Keeping the file separate makes that feature easy to
  add later without mixing it into reader.js or storage.js.
*/
