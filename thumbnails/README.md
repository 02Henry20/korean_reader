# Thumbnails

Place external story thumbnail images here when a story JSON uses a path such as:

```json
"thumbnail": "thumbnails/example.jpg"
```

The application also generates an SVG fallback when no external thumbnail exists.
